{
  "name": "tree-sitter-dbt-jinja",
  "version": "1.0.0",
  "description": "# This installs the Tree-sitter CLI itself",
  "main": "bindings/node",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nan": "^2.14.2"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.19.4"
  }
}
